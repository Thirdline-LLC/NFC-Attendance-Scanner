Here is the targeted prompt to give Claude Code to fix the grade calculation:

> "In `artifacts/nfc-attendance-scanner/src/lib/attendance-export.ts` (and any related store/export utilities), fix the grade calculation logic when exporting attendance records:
> 
> 
> * **The Bug**: The spreadsheet export is calculating the student grade as one year too low (e.g., showing Grade 11 instead of Grade 12 for the Class of 2027 during the 2026–2027 school year).
> * **Root Cause**: The calculation likely computes `12 - (gradYear - currentYear)` using the calendar year (`2026`) instead of the **academic school year**, or fails to advance the school year when the current month is August/September or later. In Fall 2026, the graduating class of 2027 is actively in **Grade 12** (Senior year).
> * **Expected Logic**:
> * Determine the effective graduation year of the current senior class based on the scan date:
> * If the scan month is August or later (Month $\ge$ 7, 0-indexed), `currentSeniorGradYear = scanYear + 1`.
> * If the scan month is before August (January–July), `currentSeniorGradYear = scanYear`.
> 
> 
> * Compute `grade = 12 - (studentGradYear - currentSeniorGradYear)`.
> * For example: For a scan in September 2026 and a student graduating in 2027 (`'27'`), `currentSeniorGradYear` is `2027`, making their grade $12 - (2027 - 2027) =$ **12**.
> 
> 
> * **Testing & Verification**:
> 1. Add a Vitest test in `src/lib/attendance-export.test.ts` testing grade calculation across fall and spring dates for classes 2027, 2028, 2029, and 2030.
> 2. Run `pnpm --filter nfc-attendance-scanner exec vitest run src/lib/attendance-export.test.ts` to confirm all assertions pass."
> 
> 
> 
> 
> 
>